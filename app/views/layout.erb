<!DOCTYPE html>
<html lang="en">
<head>
  <link rel="stylesheet" href="/css/normalize.css">

  <link rel="stylesheet" href="/css/application.css">
  <link href='http://fonts.googleapis.com/css?family=Miniver' rel='stylesheet' type='text/css'>
  <script src="//ajax.googleapis.com/ajax/libs/jquery/1.9.1/jquery.min.js"></script>
  <script src="/js/application.js"></script>
  <title></title>
</head>

<body>
  <div class="navbar">
    <%if session[:user_id] %>
      <a href="/<%= User.find(session[:user_id]).username %>"><img src='<%= User.find(session[:user_id]).gravatar_url %>' id="profile_button"> <%=User.find(session[:user_id]).username %></a>
      <form action="/logout" method="post" class="message" id="logout">
        <input type="submit" value="Logout">
      </form>
    <%end%>
  <div class="message">
    <%= erb :search %>
  </div>
</div>
  <%= yield %>
</body>
</html>
